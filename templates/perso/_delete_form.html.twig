<form method="post" action="{{ path('app_perso_delete', {'id': perso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perso.id) }}">
    <button class="btn">Delete</button>
</form>
