{% extends 'base.html.twig' %}

{% block title %}Modifier le Scenario{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="mb-4">Modifier le Scenario</h1>

    {{ include('scenario/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="mb-3">
        <a href="{{ path('app_scenario_index') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left-circle"></i> Retour à la liste
        </a>
    </div>

    <div class="mt-4">
        {{ include('scenario/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
