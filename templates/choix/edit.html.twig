{% extends 'base.html.twig' %}

{% block title %}Modifier un Choix{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="mb-4">Modifier le Choix</h1>

    <div class="mb-3">
        {{ include('choix/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mb-3">
        <a href="{{ path('app_choix_index') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left-circle"></i> Retour à la liste
        </a>
    </div>

    <div class="mt-4">
        {{ include('choix/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
